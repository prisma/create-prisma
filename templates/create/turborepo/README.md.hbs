# {{projectName}}

Generated by `create-prisma` with the Turborepo template.

## Structure

- `apps/api` - Hono API app
- `packages/db` - shared Prisma package

## Scripts

{{#if (eq packageManager "bun")}}
- `bun run dev` - run API dev server through Turborepo
- `bun run build` - run package builds through Turborepo
- `bun run typecheck` - run TypeScript checks through Turborepo
- `bun run db:generate` - generate Prisma Client in `packages/db`
- `bun run db:migrate` - create/apply migrations in `packages/db`
- `bun run db:seed` - run seed script in `packages/db`
{{else}}
{{#if (eq packageManager "pnpm")}}
- `pnpm dev` - run API dev server through Turborepo
- `pnpm build` - run package builds through Turborepo
- `pnpm typecheck` - run TypeScript checks through Turborepo
- `pnpm db:generate` - generate Prisma Client in `packages/db`
- `pnpm db:migrate` - create/apply migrations in `packages/db`
- `pnpm db:seed` - run seed script in `packages/db`
{{else}}
{{#if (eq packageManager "npm")}}
- `npm run dev` - run API dev server through Turborepo
- `npm run build` - run package builds through Turborepo
- `npm run typecheck` - run TypeScript checks through Turborepo
- `npm run db:generate` - generate Prisma Client in `packages/db`
- `npm run db:migrate` - create/apply migrations in `packages/db`
- `npm run db:seed` - run seed script in `packages/db`
{{else}}
- `npm run dev` or `pnpm dev` or `bun run dev` - run API dev server through Turborepo
- `npm run build` or `pnpm build` or `bun run build` - run package builds through Turborepo
- `npm run typecheck` or `pnpm typecheck` or `bun run typecheck` - run TypeScript checks through Turborepo
- `npm run db:generate` or `pnpm db:generate` or `bun run db:generate` - generate Prisma Client in `packages/db`
- `npm run db:migrate` or `pnpm db:migrate` or `bun run db:migrate` - create/apply migrations in `packages/db`
- `npm run db:seed` or `pnpm db:seed` or `bun run db:seed` - run seed script in `packages/db`
{{/if}}
{{/if}}
{{/if}}

## Prisma

Prisma is scaffolded inside `packages/db`:

- `packages/db/prisma/schema.prisma`
- `packages/db/prisma/seed.ts`
- `packages/db/src/client.ts`
- `packages/db/prisma.config.ts`
- `packages/db/src/generated/prisma`

`apps/api/src/index.ts` imports the shared Prisma client from `@repo/db`.
