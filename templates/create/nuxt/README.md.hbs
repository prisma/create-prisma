# {{projectName}}

Generated by `create-prisma` with the Nuxt template.

## Scripts

{{#if (eq packageManager "bun")}}
- `bun run dev` - start the Nuxt dev server
- `bun run build` - build for production
- `bun run preview` - preview the production build
- `bun run typecheck` - run Nuxt type checks
{{else}}
{{#if (eq packageManager "pnpm")}}
- `pnpm dev` - start the Nuxt dev server
- `pnpm build` - build for production
- `pnpm preview` - preview the production build
- `pnpm typecheck` - run Nuxt type checks
{{else}}
{{#if (eq packageManager "npm")}}
- `npm run dev` - start the Nuxt dev server
- `npm run build` - build for production
- `npm run preview` - preview the production build
- `npm run typecheck` - run Nuxt type checks
{{else}}
- `npm run dev` or `pnpm dev` or `bun run dev` - start the Nuxt dev server
- `npm run build` or `pnpm build` or `bun run build` - build for production
- `npm run preview` or `pnpm preview` or `bun run preview` - preview the production build
- `npm run typecheck` or `pnpm typecheck` or `bun run typecheck` - run Nuxt type checks
{{/if}}
{{/if}}
{{/if}}

## Prisma

Prisma setup is scaffolded automatically in:

- `prisma/schema.prisma`
- `prisma/seed.ts`
- `server/utils/prisma.ts`
- `server/api/users.get.ts`
- `prisma.config.ts`
- `server/generated/prisma`

Database helper scripts are added to `package.json`:

- `db:generate`
- `db:push`
- `db:migrate`
- `db:seed`
{{#if (eq schemaPreset "basic")}}

The starter page in `app/pages/index.vue` fetches seeded users from `server/api/users.get.ts`, and `prisma/seed.ts` inserts starter users.
{{/if}}
