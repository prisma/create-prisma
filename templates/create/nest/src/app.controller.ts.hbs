import { Controller, Get } from "@nestjs/common";

@Controller()
export class AppController {
  @Get()
  getRoot() {
    return {
      name: "{{projectName}}",
      framework: "NestJS",
      status: "ready",
      endpoints: {
        users: "/users",
      },
    };
  }
}
