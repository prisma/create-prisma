# {{projectName}}

Generated by `create-prisma` with the SvelteKit template.

## Scripts

{{#if (eq packageManager "bun")}}
- `bun run dev` - start the SvelteKit dev server
- `bun run build` - build for production
- `bun run preview` - preview the production build
- `bun run check` - run SvelteKit sync and type checks
{{else}}
{{#if (eq packageManager "pnpm")}}
- `pnpm dev` - start the SvelteKit dev server
- `pnpm build` - build for production
- `pnpm preview` - preview the production build
- `pnpm check` - run SvelteKit sync and type checks
{{else}}
{{#if (eq packageManager "npm")}}
- `npm run dev` - start the SvelteKit dev server
- `npm run build` - build for production
- `npm run preview` - preview the production build
- `npm run check` - run SvelteKit sync and type checks
{{else}}
- `npm run dev` or `pnpm dev` or `bun run dev` - start the SvelteKit dev server
- `npm run build` or `pnpm build` or `bun run build` - build for production
- `npm run preview` or `pnpm preview` or `bun run preview` - preview the production build
- `npm run check` or `pnpm check` or `bun run check` - run SvelteKit sync and type checks
{{/if}}
{{/if}}
{{/if}}

## Prisma

Prisma setup is scaffolded automatically in:

- `prisma/schema.prisma`
- `prisma/seed.ts`
- `src/lib/server/prisma.ts`
- `prisma.config.ts`
- `generated/prisma`

Database helper scripts are added to `package.json`:

- `db:generate`
- `db:push`
- `db:migrate`
- `db:seed`
{{#if (eq schemaPreset "basic")}}

The starter page loads users in `+page.server.ts`, renders them in `+page.svelte`, and `prisma/seed.ts` inserts starter users.
{{else}}

The starter page keeps the official SvelteKit minimal structure and points you to `prisma/schema.prisma` for your first model.
{{/if}}
